[project]
name = "transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pyarrow>=25.0.1",
    "tensorboard>=2.21.0",
    "tokenizers>=0.23.1",
    "torch>=2.13.0",
]
